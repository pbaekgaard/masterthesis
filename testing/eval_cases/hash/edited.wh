ui32 res;
ui32 stmt;
ui32 bit_shift;
ui32 flip_mask;

fn main(ui32 stmt, ui32 flip_mask) -> ui32 {
    ui32 message;
    ui32 messageDup;
    ui32 h;
    ui32 hDup;
    ui32 multiplier;
    ui32 multiplierDup;
    ui32 prime_modulo;
    ui32 prime_moduloDup;
    ui32 temp_msg;
    ui32 temp_msgDup;
    ui32 chunk;
    ui32 chunkDup;
    message = (0 as ui32);
    if (stmt == (0 as ui32)) {
        message = (0 as ui32) ^ flip_mask;
    }
    messageDup = (0 as ui32);
    if (stmt == (1 as ui32)) {
        messageDup = (0 as ui32) ^ flip_mask;
    }
    message = (12345 as ui32);
    if (stmt == (2 as ui32)) {
        message = (12345 as ui32) ^ flip_mask;
    }
    messageDup = (12345 as ui32);
    if (stmt == (3 as ui32)) {
        messageDup = (12345 as ui32) ^ flip_mask;
    }
    h = (5381 as ui32);
    if (stmt == (4 as ui32)) {
        h = (5381 as ui32) ^ flip_mask;
    }
    hDup = (5381 as ui32);
    if (stmt == (5 as ui32)) {
        hDup = (5381 as ui32) ^ flip_mask;
    }
    multiplier = (33 as ui32);
    if (stmt == (6 as ui32)) {
        multiplier = (33 as ui32) ^ flip_mask;
    }
    multiplierDup = (33 as ui32);
    if (stmt == (7 as ui32)) {
        multiplierDup = (33 as ui32) ^ flip_mask;
    }
    prime_modulo = (0 as ui32);
    if (stmt == (8 as ui32)) {
        prime_modulo = (0 as ui32) ^ flip_mask;
    }
    prime_moduloDup = (0 as ui32);
    if (stmt == (9 as ui32)) {
        prime_moduloDup = (0 as ui32) ^ flip_mask;
    }
    prime_modulo = (104 as ui32) * (1000 as ui32) + (729 as ui32);
    if (stmt == (10 as ui32)) {
        prime_modulo = (104 as ui32) * (1000 as ui32) + (729 as ui32) ^ flip_mask;
    }
    if ( message != messageDup) {
        return ((77 as ui32) as ui32);
    }
    temp_msg = message;
    if (stmt == (11 as ui32)) {
        temp_msg = message ^ flip_mask;
    }
    temp_msgDup = messageDup;
    if (stmt == (12 as ui32)) {
        temp_msgDup = messageDup ^ flip_mask;
    }
    chunk = (0 as ui32);
    if (stmt == (13 as ui32)) {
        chunk = (0 as ui32) ^ flip_mask;
    }
    chunkDup = (0 as ui32);
    if (stmt == (14 as ui32)) {
        chunkDup = (0 as ui32) ^ flip_mask;
    }
    if ( temp_msg == (0 as ui32)) {
        if ( temp_msg != temp_msgDup) {
            return ((77 as ui32) as ui32);
        }
        temp_msg = (7 as ui32);
        if (stmt == (15 as ui32)) {
            temp_msg = (7 as ui32) ^ flip_mask;
        }
        temp_msgDup = (7 as ui32);
        if (stmt == (16 as ui32)) {
            temp_msgDup = (7 as ui32) ^ flip_mask;
        }
    }
    if ( temp_msg != temp_msgDup) {
        return ((77 as ui32) as ui32);
    }
    chunk = temp_msg - temp_msg / (100 as ui32) * (100 as ui32);
    if (stmt == (17 as ui32)) {
        chunk = temp_msg - temp_msg / (100 as ui32) * (100 as ui32) ^ flip_mask;
    }
    chunkDup = temp_msgDup - temp_msgDup / (100 as ui32) * (100 as ui32);
    if (stmt == (18 as ui32)) {
        chunkDup = temp_msgDup - temp_msgDup / (100 as ui32) * (100 as ui32) ^ flip_mask;
    }
    if ( chunk != chunkDup) {
        return ((77 as ui32) as ui32);
    }
    h = h * multiplier + chunk;
    if (stmt == (19 as ui32)) {
        h = h * multiplier + chunk ^ flip_mask;
    }
    hDup = hDup * multiplierDup + chunkDup;
    if (stmt == (20 as ui32)) {
        hDup = hDup * multiplierDup + chunkDup ^ flip_mask;
    }
    if ( h != hDup) {
        return ((77 as ui32) as ui32);
    }
    h = h - h / prime_modulo * prime_modulo;
    if (stmt == (21 as ui32)) {
        h = h - h / prime_modulo * prime_modulo ^ flip_mask;
    }
    hDup = hDup - hDup / prime_moduloDup * prime_moduloDup;
    if (stmt == (22 as ui32)) {
        hDup = hDup - hDup / prime_moduloDup * prime_moduloDup ^ flip_mask;
    }
    if ( h != hDup) {
        return ((77 as ui32) as ui32);
    }
    temp_msg = temp_msg / (100 as ui32);
    if (stmt == (23 as ui32)) {
        temp_msg = temp_msg / (100 as ui32) ^ flip_mask;
    }
    temp_msgDup = temp_msgDup / (100 as ui32);
    if (stmt == (24 as ui32)) {
        temp_msgDup = temp_msgDup / (100 as ui32) ^ flip_mask;
    }
    if ( temp_msg != temp_msgDup) {
        return ((77 as ui32) as ui32);
    }
    if ( h < (0 as ui32)) {
        if ( h != hDup) {
            return ((77 as ui32) as ui32);
        }
        h = h + prime_modulo;
        if (stmt == (25 as ui32)) {
            h = h + prime_modulo ^ flip_mask;
        }
        hDup = hDup + prime_moduloDup;
        if (stmt == (26 as ui32)) {
            hDup = hDup + prime_moduloDup ^ flip_mask;
        }
    }
    if ( h != hDup) {
        return ((77 as ui32) as ui32);
    }
    return (h as ui32);
}
stmt = (? as ui32);
bit_shift = (? as ui32);
assume (bit_shift <= (31 as ui32));
assume (bit_shift >= (0 as ui32));
assume (stmt >= (0 as ui32));
assume (stmt <= (26 as ui32));
flip_mask = ((1 as ui32) << bit_shift);

res = main(stmt, flip_mask);
assert(res == (XYZ as ui32));
